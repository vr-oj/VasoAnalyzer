# Navigation Model

Use the navigation bar to set the time window (step, zoom, presets), and the overview strip to scrub across the entire recording. Use Pan/Select mode to interact directly with the plot. Additional view actions (grid, autoscale, zoom history) live in the View menu.
