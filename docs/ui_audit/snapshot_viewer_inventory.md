# Snapshot Viewer Inventory (Code-Derived)

## Search coverage
- Keywords searched: snapshot, tiff, stack, frame, page, viewer, SnapshotViewer, snapshot_view, tiff_view, ImageView.
- Inspected: `src/vasoanalyzer/ui/main_window.py`, `src/vasoanalyzer/ui/mixins/snapshot_mixin.py`, `src/vasoanalyzer/ui/dialogs/settings/frame_tab.py`, `src/vasoanalyzer/ui/_archive/*`, `src/vasoanalyzer/ui/docks/*`.
- Result: viewer-like implementations and entry points listed below; no snapshot viewer widgets in docks or `_archive`, and `frame_tab.py` is unrelated (plot settings only).

## Viewer inventory

| Name | File path | Type | Instantiated by | Data source | Sync mechanism | User entry points | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Legacy snapshot viewer (snapshot_label + slider + controls) | `src/vasoanalyzer/ui/shell/init_ui.py` (widget creation)<br>`src/vasoanalyzer/ui/main_window.py` (display_frame, change_frame, _apply_frame_change) | Embedded panel (QLabel + QSlider + playback controls) | Created in `init_ui` and inserted into `snapshot_card`/`snapshot_stack` inside `VasoAnalyzerApp.rebuild_default_main_layout` (MainWindow) | `self.snapshot_frames` list of numpy arrays loaded via `_load_snapshot_from_path` (TIFF) or `load_snapshots` (sample snapshots/assets); uses `frames_metadata`, `frame_times`, `frame_trace_time` | Slider -> `change_frame` -> `_apply_frame_change` -> `jump_to_time`; playback timer -> `_apply_frame_change` -> `jump_to_time`; trace cursor movement or event selection -> `jump_to_time` -> `set_current_frame` | View > Panels > Snapshot Viewer; Import Result TIFF (toolbar + File > Open Data); dataset load with embedded snapshots/asset; trace/event interactions update frame | Legacy display path; still active; duplicates SnapshotViewPG rendering + mapping logic |
| SnapshotViewPG (PyQtGraph ImageView) | `src/vasoanalyzer/ui/snapshot_viewer/experimental/snapshot_view_pg.py`<br>`src/vasoanalyzer/ui/main_window.py` (integration) | Widget (PyQtGraph ImageView) embedded in snapshot stack | Created in `VasoAnalyzerApp.__init__`, added to `snapshot_stack` in `rebuild_default_main_layout` | Same `snapshot_frames` stack as legacy viewer, coerced to `np.stack` in `_update_pg_snapshot_viewer`, with `frame_trace_time` passed as `xvals` | `currentTimeChanged` -> `_on_snapshot_time_changed` -> `jump_to_time`; `jump_to_time` -> `_propagate_time_to_snapshot_pg` -> `set_current_time`; slider -> `_set_snapshot_frame` -> `set_frame_index`; playback uses ImageView play/stop | View > Panels > Snapshot Viewer; View > Use PyQtGraph snapshot viewer (toggle, default on); Import Result TIFF actions; dataset load with snapshots | Best existing architecture; currently coexists with legacy viewer (toggle) |
| SnapshotMixin (legacy snapshot viewer logic) | `src/vasoanalyzer/ui/mixins/snapshot_mixin.py` | Helper mixin (legacy) | Not referenced by current classes; would be mixed into a main-window class in older architecture | `self.snapshot_frames` list from `load_tiff` or sample snapshots; metadata via `frames_metadata`, `frame_times` | Similar to main window: slider/manual stepping, playback timer, `toggle_snapshot_viewer`, metadata panel | If used, same UI actions as main window (Snapshot Viewer toggle + Import Result TIFF) | Appears unused; duplicate logic with main window (quarantine candidate) |
| PreviewPlayerWidget (GIF Animator preview) | `src/vasoanalyzer/ui/gif_animator/preview_player.py`<br>`src/vasoanalyzer/ui/gif_animator/animator_window.py` | Embedded widget inside GIF Animator window | Created in `GifAnimatorWindow._init_ui` | `rendered_frames` list from `AnimationRenderer` (numpy RGB arrays) | Slider scrubbing + QTimer playback; emits `frame_changed` when user scrubs/plays; no trace-time mapping | Export > Export GIF… opens GifAnimatorWindow (preview panel) | Viewer for rendered animation frames (not live trace sync) |
| CropSelectionDialog / CropImageLabel (GIF Animator crop ROI) | `src/vasoanalyzer/ui/gif_animator/animator_window.py` | Modal dialog (single-frame viewer + ROI selection) | Created in `GifAnimatorWindow._set_crop_roi` | First frame of `sample.snapshots` (TIFF stack) converted to QPixmap | Manual ROI selection only; no trace or event sync | GIF Animator Actions group -> Set Crop ROI | Single-frame viewer used for crop selection |
