"""Data loading and persistence helpers for VasoAnalyzer."""

# Submodules expose the concrete loader functions. Import them directly, e.g.
# ``from vasoanalyzer.io.events import load_events``. This package intentionally
# avoids implicit re-exports to keep import order predictable and prevent
# circular dependencies with ``vasoanalyzer.core``.
