# Archived UI Modules

This folder keeps legacy or parallel UI implementations that are intentionally not wired into the application.

- Purpose: preserve prior work for reference without risking regressions.
- Rule: do not import these modules from production UI paths.
